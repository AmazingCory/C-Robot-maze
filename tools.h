enum bool {false, true};
typedef enum bool bool;

enum dir {up, right, down, left};
typedef enum dir dir;

enum cndt {path, obst};
typedef enum cndt cndt;

int random_int(int, int);
int max(int, int);
int abs(int);
int print_array(int, int[100][100]);